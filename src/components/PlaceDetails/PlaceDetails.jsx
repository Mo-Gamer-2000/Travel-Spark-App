import React from "react";

const PlaceDetails = ({ place }) => {
  return (
    <div>
      <h1>{place.name}</h1>
      {/* Add more details about each place as needed */}
    </div>
  );
};

export default PlaceDetails;
